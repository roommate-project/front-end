import styled from 'styled-components';

export const TimerContainer = styled.div`
  position: relative;
  div {
    position: absolute;
    font-size: 18px;
    line-height: 48px;
    top: 0;
    bottom: 0;
    right: 30px;
  }
`;
